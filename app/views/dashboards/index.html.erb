<%= render 'search_form' %>

<h1>Dashboards#index</h1>
<p>Find me in app/views/dashboards/index.html.erb</p>

<div class="categories">
 <% @tastes.each do |movie| %>
  <a data-genre-id="<%= genre.id %>" href="#"><%= movie.genre %></a>
    <% end %>
</div>

<div>
  <% @tastes.each do |movie| %>
    <div data-genre-id="<% movie.genre.id %>"></div>
  <% end %>
</div>
